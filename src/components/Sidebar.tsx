import styles from '../dashboard.module.css';
import type { NavItem } from './types';
type SidebarProps = { active: string; items: NavItem[]; manageItems: NavItem[]; onSelect: (label: string) => void };
export default function Sidebar({ active, items, manageItems, onSelect }: SidebarProps) { return <aside className={styles.sidebar}><div className={styles.brand}><span className={styles.brandMark}>N</span>NEXUS</div><div className={styles.workspace}><span className={styles.avatar}>AK</span><div><b>Alex Kim</b><small>Personal workspace</small></div><span className={styles.chevron}>⌄</span></div><Navigation label="WORKSPACE" items={items} active={active} onSelect={onSelect}/><Navigation label="MANAGE" items={manageItems} active={active} onSelect={onSelect} manage/><div className={styles.bottom}><button className={styles.navItem}>⚙<span>Settings</span></button><button className={styles.navItem}>?<span>Help center</span></button><div className={styles.profile}><span className={styles.avatar}>AK</span><div><b>Alex Kim</b><small>alex@nexus.app</small></div></div></div></aside>; }
function Navigation({ label, items, active, onSelect, manage = false }: { label: string; items: NavItem[]; active: string; onSelect: (label: string) => void; manage?: boolean }) { return <nav className={`${styles.nav} ${manage ? styles.manage : ''}`}><small>{label}</small>{items.map((item) => <button key={item.label} className={`${styles.navItem} ${active === item.label ? styles.active : ''}`} onClick={() => onSelect(item.label)}><span>{item.icon}</span>{item.label}{item.label === 'Notifications' && <i>3</i>}</button>)}</nav>; }
